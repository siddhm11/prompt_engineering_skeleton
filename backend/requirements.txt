# fastapi>=0.104.1
# uvicorn>=0.24.0
# pydantic>=2.5.0
# python-dotenv>=1.0.0
# qdrant-client>=1.16.2
# sentence-transformers>=2.7.0
# huggingface-hub>=0.19.0
# groq>=0.9.0
# pymongo>=4.6.0

fastapi>=0.110.0
uvicorn>=0.27.1
pydantic>=2.6.1
python-dotenv>=1.0.1

# Vector DB
qdrant-client>=1.12.0

# Free embeddings (MiniLM).
#
# Do NOT re-add the [onnx] extra. As of 2026-09 it makes this file
# UNSATISFIABLE, and pip does not fail — it backtracks through the entire
# release history of transformers and tqdm, downloading an 11 MB wheel every
# ~30s, so the Space build hangs for hours instead of erroring:
#
#   sentence-transformers 6.x  requires  transformers >=5.0,<6.0
#   sentence-transformers[onnx] -> optimum-onnx 0.1.0
#   optimum-onnx 0.1.0         requires  transformers >=4.36,<4.58
#
# Those ranges do not intersect. Without the extra this file resolves in ~7s.
#
# Nothing is lost in output quality: ONNX and torch are two runtimes for the
# same MiniLM weights and produce the same vectors. The ONNX path was only
# faster per call, and llm_service.preload_embedding_model already falls back
# to the torch backend automatically. torch is a dependency either way.
sentence-transformers>=5.0.0,<7.0.0

# APIs
groq>=0.11.0
httpx>=0.24.1

# DB
pymongo>=4.6.1




# Auth & Utilities
pyjwt>=2.8.0
requests>=2.31.0

# File Upload (voice-to-prompt)
python-multipart

# Rate Limiting
slowapi>=0.1.9